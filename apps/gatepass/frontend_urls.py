from django.urls import path
from django.views.generic import TemplateView

app_name = 'gatepass'

urlpatterns = [
    # Frontend routes are handled by Next.js
    # These are placeholders if needed for server-side rendering
]
